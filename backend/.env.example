# Configurações do Flask
SECRET_KEY=asdf#FGSgvasgf$5$WGT
JWT_SECRET_KEY=jwt-secret-string-change-in-production
PORT=5000

# Configurações do Firebase
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Credentials (use one of these methods)
# Method 1: Base64 encoded JSON (recommended for production)
FIREBASE_CREDENTIALS_B64=

# Method 2: Raw JSON string (recommended for production)
FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"seu-projeto",...}

# Method 3: Path to service account JSON file (development)
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

# Configurações do usuário admin (criado automaticamente)
ADMIN_NAME=Administrador
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@relampago.com
ADMIN_PASSWORD=admin123

